{
  "name": "johndcruz-app",
  "version": "1.0.0",
  "description": "John D Cruz — Counselor & Public Servant Full-Stack App",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\" --names \"API,WEB\" --prefix-colors \"blue,green\"",
    "start": "node server/index.js",
    "build": "cd client && npm run build",
    "server": "node server/index.js",
    "client": "cd client && npm run dev",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
